# Future React App

This directory is reserved for the React + TypeScript + Vite frontend.

The current environment does not have Node.js installed, so the runnable demo UI is served from the FastAPI app instead.

Planned migration:

1. Move static dashboard flows into React pages
2. Replace inline fetch calls with typed SDK hooks
3. Add React-Konva annotation editor
4. Add plugin management and benchmark pages
